import React from 'react';

// This file is overwritten in-place by test/hmr.e2e.test.ts to simulate a
// live edit, then restored afterward. Keep it minimal and self-contained —
// no imports beyond React, so the only thing that changes between runs is
// this text content.
export default function App() {
    return <h1 id="probe">PROBE_INITIAL</h1>;
}
